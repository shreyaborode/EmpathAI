import 'package:flutter/material.dart';

class WeeklyResultScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Weekly Results')),
      body: Center(
        child: Text('Weekly Results Page (To be implemented)', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
